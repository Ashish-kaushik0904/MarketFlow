1:- div(container) -> div(row) -> div(col-6)
  -> This "col-6" is divide the page into two parts, like on page i want to add two content side by side,
     i can divide my page like this, col-12 is full width of that div(row) , so by "col-6" we can devide
     that 12 into two parts. Also we can nastes this, if we want to make another two part inside anyone 
     of them.

2:- fs(font-size):- This is just like hading tage "fs-1" is biggest, "fs-2" small then "fs-1", same as
    "h1" and "h2"

3:- mx:- if somewhere "mr" not work as "margin right" this not work we use "mx"(horizontal margin)         